import { ConditionMapPipe } from './condition-map.pipe';

describe('ConditionMapPipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionMapPipe();
    expect(pipe).toBeTruthy();
  });
});
