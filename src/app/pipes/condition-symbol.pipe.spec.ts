import { ConditionSymbolPipe } from './condition-symbol.pipe';

describe('ConditionSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
