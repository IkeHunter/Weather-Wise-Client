import { ConditionValuePipe } from './condition-value.pipe';

describe('ConditionValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionValuePipe();
    expect(pipe).toBeTruthy();
  });
});
