import { ConditionsPipe } from './conditions.pipe';

describe('ConditionsPipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionsPipe();
    expect(pipe).toBeTruthy();
  });
});
