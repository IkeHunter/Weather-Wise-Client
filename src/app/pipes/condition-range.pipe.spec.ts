import { ConditionRangePipe } from './condition-range.pipe';

describe('ConditionRangePipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionRangePipe();
    expect(pipe).toBeTruthy();
  });
});
